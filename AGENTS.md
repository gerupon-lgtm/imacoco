# いまここインフォ

## 概要

日本国内の現在地から、日時・地名・概算標高・天気・太陽・概算潮汐・役所・最寄り駅・医療機関を一画面で示す、非商用・ログイン不要の静的PWA。要件IDの正典はルートの要件定義書とする。

## 前提

- 政令指定都市では区役所を管轄役所として優先し、市役所を補助導線にする（実装済み）。
- PWAアイコンは現在地ピンを基調としたアプリ内SVGを使う。
- 画面内の情報・操作アイコンは`src/components/AppIcon.tsx`のアプリ内SVGへ統一し、絵文字や外部アイコン配信を追加しない。意味は必ず文字ラベルでも伝える。
- 【要確認】公開ホスト名とJ-PlatPat最終確認は公開タスクで確定する。

## 技術スタック

- Node.js 24 LTS、npm
- React、TypeScript、Vite
- PWA: vite-plugin-pwa（更新通知を画面で制御）
- 入力検証: Zod
- 端末内保存: IndexedDB。設定だけlocalStorage。アプリ資産はCache Storage
- テスト: Vitest、Testing Library、Playwright
- 配信: Cloudflare Pages、公開GitHubリポジトリ`gerupon-lgtm/imacoco`連携
- DB／独自API／認証: なし

依存パッケージは実装開始時の互換する安定版を採用し、package-lock.jsonで固定する。

## ディレクトリ構成

- src/components: 表示専用コンポーネント
- src/providers: 外部サービスアダプター
- src/domain: 正規化型、距離・方角・潮汐計算
- src/storage: IndexedDB、localStorage、キャッシュ期限・全消去
- src/data: 国土地理院自治体コードの生成済みマスター
- public/data: 役所、駅、医療の分割済み静的データ
- scripts: 公開データの取得・変換・検証・版整合
- e2e: 利用者フロー
- docs: 設計の正典

## 規約

- 外部API応答をUIへ直接渡さず、Zodで検証してdomain型へ正規化する。
- 各カードはidle/loading/fresh/cached/stale/error/not_applicableを持ち、他カードの失敗から独立させる。
- GPS座標をURL、ログ、解析、例外メッセージへ含めない。
- 実装時はdocs/tasks.mdのタスクIDを作業記録へ含める。
- 要件を変える場合は設計だけを直さず、要件定義書へ【失効】と改訂履歴を追記する。

## ブランド表記

- 画面の主タイトルは正確に「いまここインフォ」とする。運営屋号「シクミラボ」を主タイトルへ前置しない。
- 主タイトル直下に「© 2026 SIKUMI LAB」を小さく控えめに表示する。大文字小文字、記号、空白を変えない。
- 画面フォントは同梱した小杉ゴシックを使用する。外部フォントCDNへ実行時接続せず、日本語サンセリフ系システムフォントをフォールバックにする。
- 現在日付と時計は同じ文字サイズ・太さにする。カード順は現在地、最寄り駅、天気、太陽、潮の目安、役所、医療機関とする。

## バージョン管理

- 形式: mvp-MAJOR.MINOR.PATCH
- 現在版: mvp-0.2.2
- 増分: PATCH=不具合修正、MINOR=後方互換のある機能追加、MAJOR=保存形式等の破壊的変更
- リセット: MINOR更新時はPATCHを0、MAJOR更新時はMINOR/PATCHを0
- 正典: package.jsonのversion。表示時にmvp-を付ける
- 反映先: フッター、PWAキャッシュ名、ビルド情報、設計サマリ
- 同値検証: scripts/check-version.mjsでpackage.json、生成定数、表示、キャッシュ名を検査する

## 日時・タイムゾーン

- 基準タイムゾーンはIANA IDのAsia/Tokyo。画面表示は要件に従いJST固定
- API交換・端末内の時刻付き日時はUTCのISO 8601で保存する
- Open-Meteoへはtimezone=Asia/Tokyoを指定し、応答のtimezoneも検証する
- 「今日」と日付境界はJSTローカル日付で判定する
- ヘッダーの現在時計はJSTのHH:mmで少なくとも毎分更新し、現在日付はYYYY年M月D日（曜）でJST 0時に切り替える
- タブのバックグラウンド復帰時は経過時間の加算ではなく現在時刻から再計算し、時計のずれを補正する
- 現在時計とGPS取得時刻・各カード更新時刻は、ラベルと配置で混同させない
- 文字列日時の暗黙解釈、サーバーのローカル時刻依存、+09:00の手動加算を禁止する
- CIはTZ=UTCで実行し、JST 0時前後、月末月初、UTC/JSTで日付が異なる時刻をテストする

## コマンド

- 開発: npm run dev
- 型検査: npm run typecheck
- 単体・結合: npm test
- E2E: npm run test:e2e
- 静的データ検証: npm run data:validate
- 本番ビルド: npm run build
- 全確認: npm run check

## 実装上の注意

- 初回だけ説明後にGPS許可を求め、以後は許可済みなら起動時更新する。
- 地名・役所・駅・医療は250m以内、天気・海況は1km以内かつ15分以内だけ再利用する。
- 最新位置・表示スナップショットは1件だけ保持し、24時間で期限切れにする。
- 外部通信は10秒で打ち切り、一時エラーだけ1回再試行する。最大20秒後はカード単位の失敗または前回値を示す。
- 共有本文に緯度経度を初期状態で含めない。
- 概算標高はOpen-Meteo Weather APIの既存応答だけを使い、専用通信を追加しない。「約」「概算」を外さず、欠損時は標高行だけを隠す。

## 変更禁止事項（根拠つき）

- 潮汐を正確な満干潮として表示しない。海面高度は潮汐以外の影響も含み、沿岸精度に限界があるため。
- 医療機関を「診療中」「救急受入可」と独自判定しない。公式データでも臨時変更を保証できないため。
- 駅の直線距離を徒歩距離・所要時間として表示せず、時刻表や運行状況を静的データから推定しない。国土数値情報は基準日時点の位置・変遷データであるため。
- 1つの外部障害で画面全体をエラーにしない。FR-010〜FR-012の中核要件であるため。
- アクセス解析、広告Cookie、位置履歴、サーバー保存を追加しない。0円運用とプライバシーの確定要件であるため。
- 住所・座標入力、埋め込み地図をMVPへ戻さない。要件定義書で【失効】または対象外にしたため。

## 検証済みの事実

| 事実 | 検証方法 | 根拠 | 確認日 |
|---|---|---|---|
| 国土地理院の逆ジオコーダーが自治体コードと町丁目をJSONで返す | 東京駅座標へのHTTPS GET | muniCd=13101、lv01Nm=丸の内一丁目 | 2026-08-11 |
| 同エンドポイントがブラウザ直接通信を許可する | Origin付き応答ヘッダー確認 | Access-Control-Allow-Origin: * | 2026-08-11 |
| Open-Meteo Marineに潮汐を含む海面高度がある | 公式API仕様確認 | sea_level_height_msl | 2026-08-11 |
| 医療機関CSVに施設ID・名称・種別・住所・座標・公式URLがある | 2026-06-01病院施設CSVのヘッダー確認 | 厚生労働省オープンデータ | 2026-08-11 |
| 国土数値情報の2025年度鉄道時系列データが駅点・駅名・路線名・運営会社・設置期間を提供し、貨物駅・信号場・索道を対象外としている | 国土交通省のデータ詳細・利用条件を確認 | N05、2025年度版、非商用 | 2026-08-11 |
| 全国静的データがmanifestと全件一致する | `npm run data:validate` | 自治体1,919、駅9,119、役所1,960、医療189,800 | 2026-08-11 |
| 本番PWAビルドがService Workerと静的データを生成する | `npm run build` | 重複なしのprecache 10件（確定アイコン4資産を含む）、全国JSONはオンデマンド配信 | 2026-08-11 |

## 参照

- 要件定義書: 要件定義書_いまここインフォ.md
- データ: docs/data-model.md
- 画面: docs/screens.md
- 外部処理: docs/api-design.md
- タスクとトレーサビリティ: docs/tasks.md
- 人向け概要: 基本設計サマリ.md
